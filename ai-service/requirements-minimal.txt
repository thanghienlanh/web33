# Minimal requirements - chỉ cần để chạy AI service cơ bản
# Không bao gồm Stable Diffusion (cần GPU và nhiều RAM)

fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pillow>=10.2.0
python-dotenv>=1.0.0
requests>=2.31.0
aiofiles>=23.2.1

# Optional: For DALL-E API
openai>=1.3.0

